# Core API
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# HTTP client (audit + simulator)
httpx==0.28.1

# Config
python-dotenv==1.0.1

# Tests
pytest==8.3.4

# --- Optional integrations (uncomment when wiring real providers) ---
# openai==1.59.6        # LLM_PROVIDER=openai
# anthropic==0.42.0     # LLM_PROVIDER=anthropic
# stripe==11.4.1        # CONTEXT_PROVIDER=stripe
